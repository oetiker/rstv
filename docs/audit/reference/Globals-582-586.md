# Globals: vmtHeaderSize, voXXXX, vsXXXX, wfXXXX, WindowColorItems, wnNoNumber, WordChars, WordRec, wpXXXX  (guide pp. 582–586)

Rust module(s): `src/validate.rs`, `src/window/window.rs`, `src/widgets/editor.rs`   |   magiblot: `include/tvision/validate.h`, `include/tvision/views.h`, `source/tvision/twindow.cpp`, `source/tvision/teditor.cpp`

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `vmtHeaderSize` (constant, `= 8`) | 582 | NOT-PORTED | — | — | N/A | Pascal/Borland OOP VMT internal offset used by the `Objects` stream machinery. No equivalent in Rust — the VMT concept and the TStreamable/TObject stream layout are both dropped (deviation D12). The value `8` was the Pascal virtual-method-table header size; it has no meaning in Rust's vtable model. |
| `voFill` ($0001) | 582 | EQUIVALENT | OK | `PXPictureValidator::auto_fill: bool` (field); `suppress_fill` param on `Validator::is_valid_input` | 2 | Guide: bit in the `Options` word indicating whether the validator fills in literal characters while typing. Rust collapses the per-instance `Options` word into named boolean fields: `PXPictureValidator.auto_fill` carries the fill behaviour; `is_valid_input(s, suppress_fill)` lets callers suppress it per call. Known idiomatic mapping: flag word → struct-of-bools (deviation D5). Heritage section in module doc names the correspondence but the individual fields lack a "how/when to set" note. |
| `voTransfer` ($0002) | 582 | EQUIVALENT | OK | `Validator::transfer_get` / `transfer_set` trait methods; `RangeValidator::set_transfer(bool)` | 2 | Guide: bit indicating the validator handles data transfer for the input line (used by range validators). Rust replaces the bit with the `transfer_get`/`transfer_set` trait methods; `RangeValidator::set_transfer(enabled)` is the runtime toggle. Named and explained in the module doc and the `RangeValidator` struct doc, but "how transfer interacts with the dialog gather/scatter walk" could be more explicit. |
| `voReserved` ($00FC) | 582 | NOT-PORTED | — | — | N/A | Borland-reserved mask bits; no semantic content for user code. Not ported — Rust has no `Options` word, so these reserved bits have nowhere to live and no meaning to carry. |
| `vsOK` (0) | 583 | EQUIVALENT | OK | `Validator::is_status_ok() -> bool` (returns `true`) | 2 | Guide: status constant indicating the validator was constructed properly. Rust encodes validator construction status as the boolean `is_status_ok()` trait method; a well-formed validator always returns `true`. `PXPictureValidator` sets an internal `status_ok` field at construction time. Heritage section mentions `vsSyntax`; `vsOK` is the implied baseline. Doc explains what the method does but not the interaction with `InputLine::Valid`. |
| `vsSyntax` (1) | 583 | EQUIVALENT | OK | `PXPictureValidator::is_status_ok() -> bool` (returns `false` when mask is malformed) | 2 | Guide: status constant indicating a picture validator's mask had a syntax error. Rust: `PXPictureValidator::new` runs `syntax_check` on construction; if it fails, `status_ok = false` and `is_status_ok()` returns `false`. The `vsSyntax` comments appear in `validate.rs` line 1465/1472 in the mask-engine tests. Doc explains what `is_status_ok` does; the link to `cmValid` / `InputLine::Valid` is not drawn explicitly. |
| `wfMove` ($01) | 583 | EQUIVALENT | OK | `tv::WindowFlags { move: bool }` | 3 | Guide: window-flag bit meaning the window can be moved. Rust: `WindowFlags.r#move` bool (field named with raw identifier because `move` is a Rust keyword). Struct doc notes the keyword spelling. Known idiomatic mapping: flag word → struct-of-bools (deviation D5), documented via `# Turbo Vision heritage` section. |
| `wfGrow` ($02) | 583 | EQUIVALENT | OK | `tv::WindowFlags { grow: bool }` | 3 | Guide: window-flag bit meaning the window can be resized (has a grow icon). Rust: `WindowFlags.grow`. Same deviation D5 struct-of-bools mapping. |
| `wfClose` ($04) | 583 | EQUIVALENT | OK | `tv::WindowFlags { close: bool }` | 3 | Guide: window-flag bit meaning the window has a close icon. Rust: `WindowFlags.close`. Same deviation D5 struct-of-bools mapping. |
| `wfZoom` ($08) | 583 | EQUIVALENT | OK | `tv::WindowFlags { zoom: bool }` | 3 | Guide: window-flag bit meaning the window has a zoom icon. Rust: `WindowFlags.zoom`. Same deviation D5 struct-of-bools mapping. |
| `WindowColorItems` (function) | 583 | NOT-PORTED | — | — | N/A | C++ `ColorSel` unit function that builds a linked list of `TColorItem` records for window objects, used by the interactive colour-selector dialog (`TColorDialog`). The entire `TColorDialog` / `TColorGroup` / `TColorItem` colour-editor subsystem is not ported (it targets the built-in 16-colour EGA palette editor; deviation D7 replaces palette indexing with `Theme`/`Role`). No Rust analog exists — colour customisation routes through `Theme`, not a runtime colour-picker dialog. |
| `wnNoNumber` (constant, `= 0`) | 584 | EQUIVALENT | OK | `Window::new(…, number: i16)` with `number = 0` → `View::number() -> None`; `number_to_option(0) -> None` | 2 | Guide: sentinel value meaning the window is not numbered and cannot be selected by Alt+n. Rust: `Window.number: i16`; the private helper `number_to_option` maps `n <= 0` to `Option::None`; `View::number()` returns `None` for unnumbered windows. The mapping is tested (`view_number_some_when_positive_none_when_zero`). The public API (`Window::new` takes an `i16` and `0` is the conventional "no number") is documented in the test comment but not in the `new` doc itself. |
| `WordChars` (variable, `set of Char`) | 584 | EQUIVALENT | OK | `is_word_char(ch: u8) -> bool` + `get_char_type(ch: u8) -> u8` in `src/widgets/editor.rs` (private) | 1 | Guide: set of characters treated as word characters by the editor (digits, letters, underscore). Rust: `is_word_char` and `get_char_type` are private free functions that hardcode the same character classification (digits, letters, underscore; everything else is whitespace/punctuation). The Pascal variable was mutable at runtime; the Rust classification is compile-time-constant, which is an idiomatic narrowing (no user needs to override it). Both functions have one-line comments; there is no public API, so a doc score of 1 is the ceiling. |
| `WordRec` (type, `record Lo, Hi: Byte`) | 584 | NOT-PORTED | — | — | N/A | Pascal utility type providing named access to the Lo and Hi bytes of a Word. Pure Pascal language artifact — Rust handles byte splitting with standard methods (`u16::to_le_bytes`, bit-shifting). No analog needed or ported. |
| `wpBlueWindow` (0) | 585 | EQUIVALENT | OK | `tv::WindowPalette::Blue` | 3 | Guide: palette selector meaning yellow-on-blue text window (the default). Rust: `WindowPalette::Blue` (the `#[default]` variant). Enum doc describes the colour mapping and the role family it selects. |
| `wpCyanWindow` (1) | 585 | EQUIVALENT | OK | `tv::WindowPalette::Cyan` | 3 | Guide: palette selector meaning blue-on-cyan window (e.g. for message windows). Rust: `WindowPalette::Cyan`. Enum variant doc names the `Role::FrameCyan*` family. |
| `wpGrayWindow` (2) | 585 | EQUIVALENT | OK | `tv::WindowPalette::Gray` | 3 | Guide: palette selector meaning black-on-gray window (dialog boxes). Rust: `WindowPalette::Gray`. Dialog constructor calls `set_palette(WindowPalette::Gray)`. Enum variant doc names `Role::FrameGray*`. |

## Summary

- PORTED: 0   EQUIVALENT: 13   NOT-PORTED: 4   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 5 (`voFill`/`voTransfer` → `PXPictureValidator` fields, `vsOK`/`vsSyntax` → `is_status_ok`, `wnNoNumber` → `Window::new` doc)   |   → concept: 0
- Notable finding: No gaps or suspect items. The four NOT-PORTEDs (`vmtHeaderSize`, `voReserved`, `WindowColorItems`, `WordRec`) are all correct non-ports — Pascal VMT artifacts and the unported colour-editor subsystem. The most actionable doc gap is `wnNoNumber`: `Window::new`'s `number` parameter should document that passing `0` means "no number" (the `wnNoNumber` sentinel), since this is the only way to create an unnumbered window.
