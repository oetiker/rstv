# TEditWindow  (guide pp. 430–432)

Rust module(s): src/widgets/editor.rs (`struct EditWindow`)   |   magiblot: include/tvision/editors.h / source/tvision/teditwnd.cpp

> TEditWindow is a TWindow subclass that owns a TFileEditor wired to two hidden scroll bars and a
> hidden TIndicator. It owns the `editor` field (a raw pointer to the inserted TFileEditor) and an
> `indicator` field. It overrides `close`, `getTitle`, `handleEvent`, and `sizeLimits`.

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `editor` (field) | 430 | EQUIVALENT | OK | `tv::EditWindow::editor_id: ViewId` | 2 | C++: `TFileEditor *editor` — a raw up-pointer to the inserted child. Rust: `editor_id: ViewId` — a process-global handle. Child is resolved via `window.child_mut(editor_id)` when needed. Known idiomatic mapping: raw pointers → `ViewId` handles (D3). Doc says what it is; "how to reach the FileEditor via downcast" could be clearer. |
| `indicator` (field) | 430 | EQUIVALENT | OK | `Indicator` child inserted by `EditWindow::new`, wired to the editor as `Option<ViewId>` | N/A | RESOLVED (was UNSURE): the guide lists `indicator` as a TEditWindow field (Borland TV 2.0). The capability is present — `new` inserts a hidden `Indicator` and wires it into the `FileEditor` as an `Option<ViewId>` handle (`editor.rs:445/1452`), exactly as the `Init` row describes. Structural placement differs from the printed guide (held by id via the editor rather than as a direct owned EditWindow sub-object, per D3 `ViewId` handles); magiblot's modern header likewise routes it through the editor. EQUIVALENT, not a field gap. Internal → doc N/A. |
| `Init` (constructor) | 430 | PORTED | OK | `tv::EditWindow::new(bounds, file_name, number)` | 2 | C++: sets `ofTileable`; inserts hidden `hScrollBar`, `vScrollBar`, `indicator`; creates `TFileEditor` wired to the three + inserts it. Rust: identical sequence — sets `options.tileable = true`; inserts hidden `ScrollBar`×2 and `Indicator` first (to obtain their `ViewId`s), then constructs `FileEditor` wired to those ids, then inserts. Geometry constants match (h-bar: rows 18..size.x-2 bottom row; v-bar: right col rows 1..size.y-1; indicator: cols 2..16 bottom row; editor grows -1 inset). |
| `close` (method) | 431 | PORTED | OK | `tv::EditWindow::handle_event` `cmClose` arm | 2 | C++: `if (editor->isClipboard()) hide(); else TWindow::close()`. Rust: in `handle_event`, when the incoming event is `cmClose` and `ctx.clipboard_editor_id() == Some(self.editor_id)`, calls `ctx.request_set_visible(id, false)` (= hide) and returns; otherwise the event falls through to `self.window.handle_event` which runs the normal close. The hide path is exact; the otherwise-close path is a fall-through rather than an explicit `TWindow::close()` call — functionally equivalent. Comment in code documents the intent. |
| `getTitle` (method) | 431 | EQUIVALENT | OK | `tv::EditWindow::new` sets the window title; title updated via `handle_event` `cmUpdateTitle` broadcast | 2 | C++: `getTitle` is called by `TFrame::draw` to paint the frame title; it returns `clipboardTitle`, `untitled`, or `editor->fileName`. Rust: no `get_title` method — the window title is stored as a `String` in `Window` and updated once in `new` (from `file_name`) and re-pushed on each `cmUpdateTitle` broadcast via `Window::set_title`. The frame reads the stored title directly. Known idiomatic mapping: push vs pull. The clipboard-title path is absent — `EditWindow` never hosts the clipboard editor in Rust's design (the clipboard editor is a plain `Editor`, not an `EditWindow`-hosted `FileEditor`). This is a deliberate architectural decision; the "hide instead of close" guard in `close` would handle it if it ever were hosted. Doc score 2 — the mapping is not explicitly documented. |
| `handleEvent` (method) | 431 | PORTED | OK | `tv::EditWindow::handle_event` (impl `View::handle_event` in `#[delegate]` block) | 2 | C++: `TWindow::handleEvent(event)` then if `evBroadcast && cmUpdateTitle`: `frame->drawView()`. Rust: runs `window.handle_event` first; then checks for `Event::Broadcast { command: UPDATE_TITLE, .. }`, downcasts child to `FileEditor`, reads `file_name`, and calls `Window::set_title`. The push-title pattern replaces the pull `getTitle` + redraw. Broadcast is intentionally NOT consumed (left live) so all `EditWindow` instances can refresh their titles — this design choice is documented in the code comment. The C++ consumed the event (`clearEvent`) whereas Rust does not; this is a meaningful divergence but it is deliberate and noted. Marking OK rather than SUSPECT because the comment makes it explicit. |
| `sizeLimits` (method) | 432 | PORTED | OK | `tv::EditWindow::size_limits(&self, owner_size) -> (Point, Point)` | 2 | C++: `TWindow::sizeLimits(min, max); min = minEditWinSize` where `minEditWinSize = {24, 6}`. Rust: `View::size_limits` override returns `(Point::new(24, 6), max)` where `max` comes from delegating to `self.window`. Exact constant match. Comment in code explains why `calc_bounds` is in the `skip` list of the `#[delegate]` macro. Doc score 2 — the "24×6 minimum" is explained but not the why (editor + frame + bars require that space). |
| `palette` (7 entries: CEditWindow) | 432 | EQUIVALENT | OK | `tv::theme::Role::Frame*`, `Role::ScrollerNormal`, `Role::ScrollerSelected` (via `Window` + `Editor` delegation) | N/A | C++: `CEditWindow` palette has 7 slots: frame passive/active/icon (3), scrollbar page/controls (2), editor normal/selected text (2). Rust: `EditWindow` has no `getPalette` override; `Window` and then `Editor` provide their respective role lookups through the delegation chain. Known idiomatic mapping: class Palette → `tv::Theme`. Private concern; no public Rust symbol. |
| `Load` / `Store` (stream) | 432 | NOT-PORTED | — | — | — | `TStreamable` dropped project-wide. Known idiomatic mapping. |

## Summary

- PORTED: 4   EQUIVALENT: 4   NOT-PORTED: 1   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 5   |   → concept: 0
- Notable findings: One UNSURE: the guide lists `indicator` as a TEditWindow field but the C++ header does not; the Rust `indicator_id: ViewId` maps it if the guide is authoritative. The most important design divergence is `handleEvent`/`cmUpdateTitle`: C++ consumed the broadcast and called `frame->drawView()` (pull-title); Rust does NOT consume it and instead pushes the new title string into `Window` — a deliberate deviation that allows all `EditWindow` instances to refresh independently, documented inline. A "# Turbo Vision heritage" note on `EditWindow::handle_event` explaining why the broadcast is left live would close the doc gap.
