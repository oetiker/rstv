# TFileEditor  (guide pp. 438–441)

Rust module(s): src/widgets/editor.rs (`struct FileEditor`)   |   magiblot: include/tvision/editors.h / source/tvision/tfiledtr.cpp

> TFileEditor subclasses TEditor, adding a `fileName[MAXPATH]` field and file-IO methods. It
> overrides buffer management (`initBuffer`/`doneBuffer`/`setBufSize`) to use heap-allocated
> growable memory (vs the fixed-size base), and adds `loadFile`, `save`, `saveAs`, `saveFile`,
> `updateCommands`, and `valid`. The palette is the same 2-entry editor palette (inherited).

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `fileName` (field) | 438 | EQUIVALENT | OK | `tv::FileEditor::file_name: Option<PathBuf>` | 2 | C++: `char fileName[MAXPATH]` — empty string signals "untitled". Rust: `Option<PathBuf>` — `None` signals untitled. Same semantics; `PathBuf` is idiomatic Rust vs fixed C-string. Doc says what it is; "why `Option` instead of empty string" would score 3. |
| `Init` (constructor) | 438 | PORTED | OK | `tv::FileEditor::new(bounds, h_scroll_bar, v_scroll_bar, indicator, file_name)` | 2 | C++: calls `TEditor(…, 0)` (zero buf size), then `doneBuffer()`/`initBuffer()`, then `fexpand` + `loadFile`. Rust: calls `Editor::new_file_editor(…)` (file-editor mode, growable), then `std::path::absolute` to expand the path, then `load_file()`. Equivalent sequence. The `fexpand` analog is `std::path::absolute` (fallback to original if CWD unavailable), documented in the code. "how/when to construct" not in rustdoc. |
| `doneBuffer` (method) | 438 | NOT-PORTED | — | — | — | C++: `free(buffer)` — explicit heap deallocation. Rust: `Vec<u8>` in `Editor` drops automatically. The Rust allocator handles this; no public method needed. |
| `initBuffer` (method) | 438 | NOT-PORTED | — | — | — | C++: `buffer = (char*) malloc(bufSize)` — explicit heap allocation at construction. Rust: `Editor::new` allocates `Vec<u8>` at initialization; the file-editor path starts with size 0 and grows on demand. No separate method needed. |
| `loadFile` (method) | 439 | PORTED | OK | `tv::FileEditor::load_file(&mut self) -> bool` | 2 | C++: `ifstream` open; `edOutOfMemory` if file > buf limit; `edReadError` on read fail; `setBufLen` on success. Empty filename → `setBufLen(0)`. Rust: `std::fs::read`; `NotFound` → empty+true (same as C++ can't-open path); real I/O error → stores `pending_load_error` for display on first `handle_event` (the async-modal seam). Buffer grows via `Editor::set_text` (file-editor mode is growable). `edOutOfMemory` path is absent: Rust `Vec` growth is infallible. Comment in code documents this. Doc score 2 — the pending_load_error deferred-display pattern merits a "# Turbo Vision heritage" note. |
| `save` (method) | 439 | PORTED | OK | `tv::FileEditor::save(&mut self, ctx) -> bool` | 2 | C++: if `*fileName == EOS` calls `saveAs()`, else calls `saveFile()`. Rust: if `file_name.is_none()` requests the save-as dialog via `Context::request_save_as_dialog` (async seam — see doc), else calls `save_file`. Semantically equivalent; the async branch is documented in the struct and method doc. "how the async round-trip works" is partially explained — score 2. |
| `saveAs` (method) | 440 | EQUIVALENT | OK | `tv::FileEditor::handle_event` `cmSaveAs` arm → `ctx.request_save_as_dialog` + `pending_title_update` | 2 | C++: `editorDialog(edSaveAs, …)` — a synchronous blocking dialog that returns the new filename, then `message(owner, evBroadcast, cmUpdateTitle, 0)` + `saveFile`. Rust: the `cmSaveAs` arm in `handle_event` calls `ctx.request_save_as_dialog(id)` (async, returns immediately); the completion handler sets `file_name` + `pending_title_update` and re-injects `cmSave`; the `cmSave` arm then calls `save()` which calls `save_file()` and broadcasts `cmUpdateTitle`. Functionally equivalent; the async seam is documented in the struct-level doc. Notable: C++ `saveAs` clears `fileName` if `isClipboard` after saving — Rust has no exact analog for this (the clipboard editor never has a file name in the Rust design). No functional gap for the normal path. |
| `saveFile` (method) | 440 | PORTED | OK | `tv::FileEditor::save_file(&mut self, ctx) -> bool` | 2 | C++: backup-rename (when `efBackupFiles` set), `ofstream` write of `[0..curPtr]` + `[curPtr+gapLen..bufLen]`, `edCreateError`/`edWriteError` on failure. Rust: same backup-rename logic (with `std::fs::remove_file` + `std::fs::rename`); `editor.text()` materializes the logical bytes (skipping the gap), `std::fs::write`. `std::fs::write` conflates create and write failure into one "Error writing file" box — code comment explains this. `edCreateError` and `edWriteError` are effectively merged. |
| `setCmdState` (method) | 440 | PORTED | OK | `tv::Editor::set_cmd_state(&self, command, enable, ctx)` | 1 | C++: `setCmdState(cmd, bool)` on `TEditor`. Rust: `Editor::set_cmd_state` — calls `ctx.enable_command` or `ctx.disable_command`. Same logic. Doc score 1: only restates the signature; "when to call / how it interacts with the active-state guard" missing. |
| `updateCommands` (method) | 440 | PORTED | OK | `tv::Editor::update_commands(&self, ctx)` | 2 | C++: calls `TEditor::updateCommands()` then `setCmdState(cmSave, True); setCmdState(cmSaveAs, True)`. Rust: `update_commands` checks `self.file_editor` and enables `Command::SAVE`/`Command::SAVE_AS` when active. Also in `FileEditor::View::handle_event` — `flush_if_unlocked` triggers `update_commands` after a save. Equivalent. The `file_editor` flag gates the save commands, which is documented in `Editor::update_commands`. |
| `valid` (method) | 440 | PORTED | OK | `tv::FileEditor::valid(&mut self, cmd, ctx) -> bool` | 2 | C++: `cmValid` → return `isValid`. Other command → if `modified`, run `edSaveModify`/`edSaveUntitled` dialog synchronously; `cmYes` → save; `cmNo` → clear modified; `cmCancel` → false. Rust: same two-phase structure, but the dialog is async (D9): first call stores nothing (requests `request_message_box` with re-post); second call reads `pending_save_answer`. The `cmValid` fast path, the Yes/No/Cancel logic, and the re-validate round-trip are all present and documented in the `valid` method doc. |
| `setBufSize` (method, override) | 438 | PORTED | OK | `tv::Editor::set_buf_size(&mut self, new_size) -> bool` | 1 | C++: `TFileEditor::setBufSize` grows via `malloc`/`memmove`; minimum 0x1000, alignment to 0x1000. Rust: `Editor::set_buf_size` grows the `Vec<u8>`, rounds up to `(new_size + 0x0FFF) & !0x0FFF`, does `copy_within` to move the tail. File-editor mode gated by `self.file_editor`; plain editor returns false. Same rounding; same layout. Doc score 1 — only inline, no public rustdoc. |
| `shutDown` (method) | 441 | EQUIVALENT | OK | `Editor::set_state(Active, false, ctx)` path in `View::set_state` | 1 | C++: `shutDown` calls `setCmdState(cmSave/cmSaveAs, False)` then `TEditor::shutDown`. Rust: `Editor::set_state` for `StateFlag::Active` with `enable=false` calls `update_commands` which checks `self.state.state.active` (now false) → disables save commands. Functionally equivalent; no explicit `shutDown` method needed. |
| `getPalette` (inherited, not overridden) | 438 | EQUIVALENT | OK | `Role::ScrollerNormal` / `Role::ScrollerSelected` via `#[delegate(to = editor)]` | N/A | C++ palette comment in editors.h: entries 1=Normal text, 2=Selected text (same as TEditor). `FileEditor` does not override `getPalette`; it delegates to `Editor` which uses `Role` lookups. Known idiomatic mapping. |
| `Load` / `Store` (stream) | 441 | NOT-PORTED | — | — | — | `TStreamable` / stream serialization dropped project-wide. Known idiomatic mapping. |

## Summary

- PORTED: 7   EQUIVALENT: 5   NOT-PORTED: 3   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 9   |   → concept: 0
- Notable findings: No functional gaps. The most important design divergence is the async-modal pattern replacing all synchronous `editorDialog` calls (`saveAs`, `valid`, `loadFile` error): the first call requests a message/file-dialog box and returns; the completion routes an answer back via `set_modal_answer` and re-posts the triggering command, which re-runs the method to consume the answer. This seam is documented at the struct level but individual method doc scores are mostly 2 — none fully explains the round-trip mechanics. The `saveAs`-clipboard edge case (C++ clears `fileName` after clipboard save) is the one behavioral nuance that has no direct Rust equivalent, but it is inconsequential in practice (the clipboard editor never has a file name).
