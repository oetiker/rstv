# TInputLine  (guide pp. 460–464)

Rust module(s): `src/widgets/input_line.rs`   |   magiblot: `include/tvision/dialogs.h` / `source/tvision/tinputli.cpp`
Validator hook: `src/validate.rs`   |   Value protocol: `src/data.rs`

> The guide documents 7 public fields (CurPos, Data, FirstPos, MaxLen, SelEnd, SelStart, Validator),
> 11 methods (Init/Done, DataSize, Draw, GetData, GetPalette, HandleEvent, SelectAll, SetData, SetState,
> SetValidator, Store, Valid), and the palette CInputLine (4 entries).
> The magiblot C++ header also exposes MaxWidth, MaxChars, and the limitMode constructor parameter —
> magiblot extensions not in the 1992 Pascal guide but present in both C++ and Rust.

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `CurPos` (field) | 460 | PORTED | OK | `InputLine::cur_pos: i32` | 3 | Guide: "index to insertion point / current cursor position." Rust: byte offset, documented as such with the coordinate-model module note. Score 3: what + how/when (byte-offset caveat explained). |
| `Data` (field) | 461 | EQUIVALENT | OK | `InputLine::data: String` | 2 | Guide: `PString` pointer. Rust: owned `String` — idiomatic. The field is public and documented ("The field contents"). Could note that it is a UTF-8 byte string (not a Pascal Pascal short string). |
| `FirstPos` (field) | 461 | PORTED | OK | `InputLine::first_pos: i32` | 3 | Guide: "index to the first displayed character." Rust: explicitly documented as a *display column*, not a byte offset — the key extension over the C++ semantics. Score 3: what + how/when (column-vs-byte distinction). |
| `MaxLen` (field) | 461 | PORTED | OK | `InputLine::max_len: i32` | 2 | Guide: "maximum length allowed, excluding the length byte." Rust: byte cap, documented. The `LimitMode` enum and the two companion caps `max_width`/`max_chars` are a magiblot extension not in the 1992 guide; they are present in both C++ and Rust (`ilMaxBytes`/`ilMaxWidth`/`ilMaxChars` in dialogs.h). Doc score 2: explains what it does; "excluding the length byte" framing (Pascal artefact) not replicated, which is fine. |
| `MaxWidth` (field) | — | PORTED | OK | `InputLine::max_width: i32` | 2 | magiblot extension (not in 1992 guide). Display-width cap; `i32::MAX` ≈ unbounded. Documented what but not the "how" of the `LimitMode` interaction. |
| `MaxChars` (field) | — | PORTED | OK | `InputLine::max_chars: i32` | 2 | magiblot extension (not in 1992 guide). Grapheme-count cap; `i32::MAX` ≈ unbounded. Same score as `max_width`. |
| `SelEnd` (field) | 461 | PORTED | OK | `InputLine::sel_end: i32` | 3 | Guide: "index to the end of the selection area." Rust: byte offset, documented. Score 3: what + byte-offset coordinate model noted. |
| `SelStart` (field) | 461 | PORTED | OK | `InputLine::sel_start: i32` | 3 | Guide: "index to the beginning of the selection area." Rust: byte offset, documented. Same as `sel_end`. |
| `Validator` (field) | 461 | EQUIVALENT | OK | `InputLine::validator: Option<Box<dyn Validator>>` | 2 | Guide: `PValidator` raw pointer, nil if none. Rust: `Option<Box<dyn Validator>>` — idiomatic. Guide warns "use SetValidator rather than direct assignment." Rust exposes the field directly (pub); there is no `set_validator` method on `InputLine` itself — the guide's `SetValidator` is absorbed into the constructor `new` (pass-in pattern). See `SetValidator` row below. Doc score 2: explains what but not why `Option<Box<dyn Validator>>` vs. the raw-pointer pattern. |
| `Init` (constructor) | 461 | PORTED | OK | `InputLine::new(bounds, limit, validator, limit_mode)` + `InputLine::with_limit(bounds, limit)` | 3 | Guide: sets `sfCursorVis`, `ofSelectable + ofFirstClick`, allocates `MaxLen + 1` bytes. Rust: identical flags set, `String` replaces the heap buffer, `with_limit` is the simple convenience form. Docs explain all three limit modes and the flags. Score 3. |
| `Done` (destructor) | 462 | NOT-PORTED | — | — | — | Destructor frees `data` and `oldData` heap allocations and calls `destroy(validator)`. Rust: all fields are owned (`String`, `Option<Box<…>>`); Drop is automatic. No explicit `done`. |
| `DataSize` (method) | 462 | NOT-PORTED | — | — | — | Returns `MaxLen + 1` (or validator-computed size) for the untyped `getData`/`setData` record protocol. Rust replaces this entire untyped offset-arithmetic protocol with the typed D10 value protocol (`View::value` / `View::set_value`); `data_size` has no analog. Known idiomatic mapping: `getData`/`setData` → D10 value protocol. |
| `Draw` (method) | 462 | PORTED | OK | `InputLine::draw` (impl `View::draw`) | 2 | Guide: different colors for focused/unfocused, arrows when text exceeds width, selected text drawn with appropriate palette. Rust: faithful implementation; `Role::InputNormal` / `Role::InputArrow` / `Role::InputSelected` replace the raw palette indices (D7). The selection-redraw vs. attribute-only distinction (no attr-only paint in tvision-rs) is commented inline. Doc score 2: explains what but the "selected-text is redrawn, not attr-only" distinction could be documented. |
| `GetData` (method) | 462 | EQUIVALENT | OK | `View::value` → `FieldValue::Text(data.clone())` or validator `transfer_get` | 2 | Guide: copies `DataSize` bytes from `Data^` into `Rec`; validator may intercept. Rust: `View::value()` returns `Option<FieldValue>`; the validator's `transfer_get` is tried first, then falls back to `FieldValue::Text`. Known idiomatic mapping: `getData` → D10 value protocol. Module doc explains the protocol. Rustdoc on `value()` scores 2: what it returns but not a "how/when to gather" guide-style explanation. |
| `GetPalette` (method) | 462 | EQUIVALENT | OK | `Role::InputNormal`, `Role::InputArrow`, `Role::InputSelected` + `tv::Theme` | 2 | Guide: returns `CInputLine` (4 entries: Passive, Active, Selected, Arrow — indices 19–21 of dialog palette). Rust: `Role`-keyed theme entries selected at draw time (D7). Known idiomatic mapping: class Palette → `tv::Theme`. The `draw` method docs name the roles but there is no dedicated `get_palette` rustdoc. Score 2. |
| `HandleEvent` (method) | 463 | PORTED | OK | `InputLine::handle_event` (impl `View::handle_event`) | 2 | Guide: mouse (click/drag block mark, edge scroll, double-click select-all), keyboard (arrow/home/end/insert/delete/backspace/ctrl navigation, printable insert, Ctrl-G delete line), command (cut/copy/paste). Rust: faithful to `tinputli.cpp`; C++ mouse do-while loops become the async tracking-capture pattern (D3, documented); clipboard becomes `Deferred` (D8); keymap dispatched through `keymap::resolve_global` (deviations documented in comments). Doc score 2: module doc covers the full repertoire at the conceptual level, but the method's own rustdoc says only "the selected keyboard/mouse block." |
| `SelectAll` (method) | 463 | PORTED | OK | `InputLine::select_all(enable: bool, scroll: bool)` | 2 | Guide: sets `CurPos`, `FirstPos`, `SelStart` to 0; if `Enable` then `SelEnd = Length(Data^)`, else `SelEnd = 0`; redraws. Rust: identical logic; `scroll` arg is preserved (defaults to `true` in C++; passed explicitly in Rust). Guide's `scroll` default is preserved by callers (`set_state` passes `false`, `set_value` passes `true`). Doc: explains what + states it does NOT draw (whole tree is redrawn instead). Score 2; "how/when to call" (e.g. the difference from `set_value`) could be explicit. |
| `SetData` (method) | 463 | EQUIVALENT | OK | `View::set_value(FieldValue)` | 2 | Guide: copies `DataSize` bytes from `Rec` into `Data^`, then calls `SelectAll(True)`. Rust: `set_value(FieldValue::Text(…))` loads text, truncates to `max_len`, calls `select_all(true, true)`; validator `transfer_set` handles typed values (D10). Known idiomatic mapping. Rustdoc on `set_value` scores 2: explains the two paths but not the truncation semantics. |
| `SetState` (method) | 463 | PORTED | OK | `InputLine::set_state` (impl `View::set_state`) | 2 | Guide: calls `TView.SetState`; if `AState` is `sfSelected` or (`sfActive` and field is `sfSelected`), calls `SelectAll(Enable)`. Rust: faithful; also drives cut/copy/paste command graying on transition (B1 feature, documented in inline comment). Score 2: the `SelectAll` trigger is documented in the rustdoc but not the command-graying side-effect. |
| `SetValidator` (method) | 463 | EQUIVALENT | OK | `InputLine::new(…, validator: Option<Box<dyn Validator>>, …)` constructor param | 1 | Guide: disposes existing validator, assigns new one. Rust: validator is set at construction time via `new`; there is no `set_validator` method on `InputLine`. The guide pattern of assigning after construction is not supported — callers reconstruct or hold `Option<Box<dyn Validator>>` directly. This is a minor API shape difference, not a behaviour gap; deliberate (constructor-ownership model). Score 1: the constructor docs mention the validator param but do not explain why there is no post-construction setter. |
| `Store` / `Load` (streaming) | 463 | NOT-PORTED | — | — | — | Pascal streaming machinery. Rust drops `TStreamable` (deviation D12). |
| `Valid` (method) | 464 | PORTED | OK | `InputLine::valid(cmd: Command, ctx: &mut Context) -> bool` | 3 | Guide: with validator — `cmValid` checks `validator.status == vsOk`; `cmCancel` passes unconditionally; any other command calls `validator.validate(Data^)`, on fail calls `Select()` and returns False. Rust: faithful. `validator.status == vsOk` → `validator.is_status_ok()`. `Select()` (re-focus) → `ctx.request_focus(id)` via Deferred (D3, documented in inline comment). Score 3: what, how, when — including the three command-class branches. |
| `CInputLine` palette (4 entries) | 464 | EQUIVALENT | OK | `Role::InputNormal`, `Role::InputArrow`, `Role::InputSelected` (Theme) | 2 | Guide: `CInputLine` maps indices 1–4 to dialog palette entries 19–21 (Passive, Active, Selected, Arrow). Entries 1 and 2 both map to 19 (passive == active in this palette). Rust uses three named `Role` enum variants keyed at draw time; the two-color-for-two-states collapse is preserved (both passive and active use `Role::InputNormal`). Known idiomatic mapping: class Palette → `tv::Theme` (D7). The `Role` variants are public and documented (what), not the palette-index derivation chain (score 2). |
| `check_valid` auto-fill clamp (internal) | — | PORTED | OK | `InputLine::check_valid` lines 334–365 | N/A | The comment at line 334 ("a future mutating validator that SHRINKS data could leave cur_pos past end-of-string") is a deliberate TODO, not a deferred feature. No bundled validator shrinks. The auto-fill clamp path (`curPos >= oldLen && newLen > oldLen → curPos = newLen`) faithfully replicates `tinputli.cpp:289–291`. Classified OK — the comment documents a known limitation, not a missing behaviour. |

## Summary

- PORTED: 14   EQUIVALENT: 7   NOT-PORTED: 3   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 13   |   → concept: 0
- Notable findings: No gaps or suspect items. The most important finding is the `SetValidator` shape change: the guide's post-construction `SetValidator` method is absent — validator assignment is constructor-only in tvision-rs. This is a deliberate API difference (ownership model) but is under-documented (score 1); a short note in `InputLine::new` explaining why there is no `set_validator` would close the gap for users porting from the C++ API.
