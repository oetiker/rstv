# TCluster  (guide pp. 395–399)

Rust module(s): `src/widgets/cluster.rs`   |   magiblot: `include/tvision/dialogs.h` / `source/tvision/tcluster.cpp`

> TCluster is the abstract base for all cluster-style controls. The Rust port
> collapses the abstract base + concrete subclasses into a single engine
> (`Cluster`) branching on a closed `ClusterKind` enum, with thin
> embed-and-delegate wrappers for the named types (deviations D1, D2).

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `EnableMask` (field) | 396 | PORTED | OK | `Cluster::enable_mask: u32` | 2 | Guide: `Longint`, controls enabled state of first 32 items; default `$FFFFFFFF`. Rust: `u32`, default `0xFFFF_FFFF`. Semantics identical. Doc explains what/default but not when to call `set_button_state` vs. mutating directly. |
| `Sel` (field, read-only) | 396 | PORTED | OK | `Cluster::sel: i32` | 2 | Guide: currently selected item index, read-only. Rust: `pub` field (not gated by accessor), otherwise faithful. Doc explains what it is. |
| `Strings` (field, read-only) | 396 | EQUIVALENT | OK | `Cluster::strings: Vec<String>` | 2 | Guide: `TStringCollection` (pointer-based dynamic array). Rust: owned `Vec<String>`. Known idiomatic mapping: `TCollection` family → `Vec`/slices. Doc explains the field's role. |
| `Value` (field, read-only) | 396 | PORTED | OK | `Cluster::value: u32` | 2 | Guide: `Longint`, interpreted per subclass. Rust: `u32` (multi-state uses all 32 bits as documented). Semantics faithful. Doc explains meaning-depends-on-kind. |
| `Init` (constructor) | 396 | EQUIVALENT | OK | `Cluster::new(bounds, strings, kind)` + `CheckBoxes::new` / `RadioButtons::new` / `MultiCheckBoxes::new` | 2 | Guide: `Init(var Bounds; AStrings: PSItem)` takes a linked `TSItem` list. Rust: takes `Vec<String>` directly (idiomatic replacement for `TStringCollection`/`TSItem` chain). Initializes `value=0`, `sel=0`, `enable_mask=0xFFFF_FFFF`, sets `ofSelectable+ofFirstClick+ofPreProcess+ofPostProcess`. Rust `Options` struct sets same flags. Doc: what it does; "how to choose kind" could be added. |
| `Load` (constructor) | 397 | NOT-PORTED | — | — | — | `TStreamable` / stream load/store dropped crate-wide (deviation: serde-if-revived). |
| `Done` (destructor) | 397 | NOT-PORTED | — | — | — | Rust `Drop` is automatic; no explicit destructor needed. |
| `ButtonState` (method) | 397 | PORTED | OK | `Cluster::button_state(item: i32) -> bool` | 2 | Guide: returns enabled state of item. Rust: private helper, same logic (`item < 32 && enable_mask bit set`). Not exposed as a public method on the wrapper types. |
| `DataSize` (method) | 397 | EQUIVALENT | OK | Cluster does not impl `View::value` / `data_size`; dialog gather/scatter skips clusters | 2 | Guide: returns `SizeOf(Value)` (a `Word`, i.e. 2 bytes for `Longint`... actually returns `SizeOf(Longint)=4`). Rust: clusters intentionally opt out of `value`/`set_value` (the D10 value protocol); the module doc explicitly states this. Known idiomatic mapping: `getData`/`setData` → D10 value protocol. The `data.rs` module doc confirms clusters are excluded. |
| `DrawBox` (method) | 397 | EQUIVALENT | OK | `ClusterKind::icon()` + `Cluster::draw` (inlined) | 1 | Guide: `DrawBox(Icon: String; Marker: Char)` — called by subclass `Draw` to paint one column. Rust: inlined into `Cluster::draw` using `kind.icon()` and `marker_char()`; no separate public method. Functionally equivalent; doc score 1 because there is no public symbol to document. |
| `DrawMultiBox` (method) | 397 | EQUIVALENT | OK | `Cluster::draw` (multi branch via `multi_mark`) | 1 | Guide: `DrawMultiBox(Icon, Marker: String)` for multi-state. Rust: same draw loop, branches on `ClusterKind::MultiCheckBoxes` using `multi_mark`; no separate public method. Functionally equivalent; doc score 1 (private impl detail). |
| `GetData` (method) | 398 | EQUIVALENT | OK | cluster opt-out of D10 value protocol (see `data.rs` module doc) | 2 | Guide: `GetData(var Rec)` writes `Value` to the record. Rust: clusters do not implement `value()`/`set_value()` — explicitly documented in the module doc as "not implemented … read and written directly by their owning dialog." Known idiomatic mapping: `getData`/`setData` → D10. |
| `GetHelpCtx` (method) | 398 | EQUIVALENT | OK | `HelpCtx` offset-by-sel not modeled | 2 | Guide: returns `HelpCtx + Sel` to give per-item help contexts. Rust: `HelpCtx` is a `&'static str` identity (not an integer), so the offset-by-index scheme does not map onto it. Module doc states: "The original per-item help-context offset does not map onto tvision-rs's string-identity `HelpCtx` and is not modeled." Deliberate, commented deviation. |
| `GetPalette` (method) | 398 | EQUIVALENT | OK | `ctx.style(Role::Cluster*)` in `Cluster::draw` | 2 | Guide: returns `CCluster` palette (4+1 entries: normal, selected, shortcut×2, disabled). Rust: `Role::ClusterNormal`, `ClusterNormalShortcut`, `ClusterSelected`, `ClusterSelectedShortcut`, `ClusterDisabled` resolved via `DrawCtx::style`. Known idiomatic mapping: class Palette → `tv::Theme`. |
| `HandleEvent` (method) | 398 | PORTED | OK | `Cluster::handle_event` (impl `View::handle_event`) | 3 | Guide: handles mouse (click/drag) and keyboard (arrows, Space, Alt-hotkey, plain-letter). Rust: full implementation with `MouseDown`/`MouseMove`/`MouseUp` hold-tracking capture, four arrow navigators, accelerator scan, focused-Space. One deliberate deviation (deferred focus vs. synchronous `focus()`) is commented inline (`tcluster.cpp:283`). |
| `Mark` (method) | 398 | PORTED | OK | `Cluster::mark(item: i32) -> bool` | 2 | Guide: abstract, default returns `False`; subclasses override. Rust: non-virtual, branches on `ClusterKind`; `MultiCheckBoxes` arm returns `false` (per the C++ abstract default). |
| `MovedTo` (method) | 398 | PORTED | OK | `Cluster::moved_to(item: i32)` | 2 | Guide: virtual, moves selection bar; `HandleEvent` calls it on mouse/arrow. Rust: non-virtual, RadioButtons arm sets `value = item`, others no-op. Documented inline. |
| `MultiMark` (method) | 398 | PORTED | OK | `Cluster::multi_mark(item: i32) -> usize` | 2 | Guide: `MultiMark(Item): Byte` returns multi-state index. Rust: returns `usize`. Same packed-state read logic (with `checked_shl` overflow guard documented). |
| `Press` (method) | 398 | PORTED | OK | `Cluster::press(item: i32)` | 2 | Guide: abstract, must be overridden; `HandleEvent` calls it. Rust: non-virtual, branches on `ClusterKind`. CheckBoxes: XOR bit. RadioButtons: set value. MultiCheckBoxes: cycle state. |
| `SetButtonState` (method) | 399 | PORTED | OK | `Cluster::set_button_state(a_mask: u32, enable: bool)` | 2 | Guide: sets/clears bits in `EnableMask`; if all disabled makes cluster unselectable. Rust: identical semantics, including the `ofSelectable` recalculation. Doc explains what; usage guidance could be added. |
| `SetData` (method) | 399 | EQUIVALENT | OK | cluster opt-out of D10 value protocol | 2 | Guide: reads `Value` from record, calls `DrawView`. Rust: explicitly excluded from the value protocol (module doc). Known idiomatic mapping. |
| `SetState` (method) | 399 | PORTED | OK | `Cluster` does not override `View::set_state`; whole-tree redraw covers it | 2 | Guide: calls `TView.SetState` then `DrawView` if `sfSelected`. Rust: no override; the whole-tree redraw on every pump tick replaces per-state `DrawView` calls (deviation D9). The module comment on `SetState` in the C++ source is absent from the Rust module doc — a reader looking for the override would not find an explanation. Doc score 2 (what happens) rather than 3 (why no override). |
| `Store` (method) | 399 | NOT-PORTED | — | — | — | `TStreamable` dropped crate-wide. |
| `CCluster` palette (4 entries) | 399 | EQUIVALENT | OK | `Role::ClusterNormal`, `ClusterNormalShortcut`, `ClusterSelected`, `ClusterSelectedShortcut`, `ClusterDisabled` | 2 | Guide: `CCluster` maps 4 entries (normal, selected, shortcut×2) onto dialog palette slots 16–18. magiblot added entry 5 (disabled = slot 31). Rust has `ClusterDisabled`. Known idiomatic mapping: class Palette → `tv::Theme`. `Role` items are documented (what); the chain from palette indices to dialog slots is not carried in rustdoc. |

## Summary

- PORTED: 11   EQUIVALENT: 10   NOT-PORTED: 3   MISSING: 0   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 10   |   → concept: 0
- Notable finding: No missing or suspect items. The most important gap is that cluster `value`/`sel` fields are publicly mutable (`pub`) with no accessor, while the guide marks both as read-only — callers can corrupt state without going through `press`/`moved_to`. This is a deliberate simplification not flagged in docs.
