# TMenuView  (guide pp. 482–485)

Rust module(s): `src/menu/menu_view.rs`, `src/menu/menu_session.rs`, `src/menu/mod.rs`   |   magiblot: `include/tvision/menus.h` / `source/tvision/tmnuview.cpp`

> TMenuView is the abstract base from which TMenuBar and TMenuBox derive.
> In tvision-rs it is split: the shared data lives in `MenuViewState`,
> the shared passive behaviour in free functions (`hot_key`, `update_menu_commands`,
> `handle_event`), the shared interface in the `MenuView` trait, and the
> interactive modal loop in `MenuSession` (deviation D9).

| Guide entry | Pg | Bucket | Corr | Rust symbol / mapping | Doc | Notes |
|---|---|---|---|---|---|---|
| `parentMenu` (field) | 483 | EQUIVALENT | OK | Level stack in `MenuSession` (no field on `MenuViewState`) | N/A | Guide: pointer to the parent menu view for hierarchical navigation. Rust replaces the up-pointer with a `Vec<MenuLevel>` stack inside `MenuSession`; the parent-of relationship is encoded by stack position, not a field on the view. Deviation D3 (pointer → session stack), documented in `menu_view.rs` module doc and `menu_session.rs` module doc. Private / internal; N/A for doc score. |
| `menu` (field) | 483 | PORTED | OK | `MenuViewState::menu: Menu` | 2 | Guide: points to the TMenu record for this view. Rust: `pub menu: Menu` on `MenuViewState`. Doc describes what it holds; lacks "how session clones it at open" detail. |
| `current` (field) | 483 | EQUIVALENT | OK | `MenuViewState::current: Option<usize>` (index, not pointer) | 2 | Guide: `PMenuItem` pointer to the currently selected item. Rust: `Option<usize>` index into `menu.items`. Same idiomatic mapping as all pointer→index conversions (deviation D3, documented). Doc explains the index convention. |
| `Init` (constructor) | 483 | PORTED | OK | `MenuViewState::new` + `MenuBar::new` / `MenuBox::new` | 2 | Guide: calls `TView.Init`, sets `eventMask |= evBroadcast`. Rust: `MenuViewState::new` builds state; `evBroadcast` opt-in is replaced by unconditional broadcast fan-out in `Group::handle_event` (documented in `menu_view.rs`: "No mask/gate is ported"). The `new` doc explains construction but does not mention the broadcast deviation explicitly. |
| `Load` (constructor) | 483 | NOT-PORTED | — | — | — | TStreamable stream persistence. Dropped codebase-wide (deviation D12, `TStreamable` dropped). |
| `Execute` (method) | 483 | EQUIVALENT | OK | `MenuSession` capture handler (`menu_session.rs`) | 3 | Guide: runs the interactive menu modal loop, returns the chosen command. Rust: flattened into one `MenuSession` `CaptureHandler` on the single event loop (deviation D9). Module doc of `menu_session.rs` is comprehensive: architecture, state, keyboard, mouse, context popups — scores 3. |
| `FindItem` (method) | 484 | PORTED | OK | `menu_view::matching_item` + `menu_session` plain-hotkey arm | 2 | Guide: returns the item whose shortcut letter matches `Ch` (case-insensitive), or nil if disabled. Rust: `matching_item(menu, ke, alt=false)` in `menu_view.rs` does the same walk. Public visibility via `pub(crate)`. Doc explains what it does (2); the case-insensitivity note could be explicit. |
| `GetItemRect` (method) | 484 | PORTED | OK | `MenuView::get_item_rect` (trait method, default `Rect::new(0,0,0,0)`) | 2 | Guide: abstract — descendants must override. Rust: default in the `MenuView` trait returns an empty rect; both `MenuBar` and `MenuBox` override it. Heritage section present; could clarify the "always-override" contract. |
| `GetHelpCtx` (method) | 484 | MISSING | — | — | — | CANDIDATE GAP — needs human confirmation. Guide: a dropped menu's `getHelpCtx` walks the `parentMenu` chain to return the highlighted item's `helpCtx`, so the status line shows per-item help while navigating a menu. Verified (`rg`): no `get_help_ctx` override on any menu type; `Group::get_help_ctx` (group.rs:904) bubbles to the *current child*, but the open menu is a `MenuSession` modal capture, not a focused menu-view subtree, so the highlighted **item's** help context is never surfaced. The `parentMenu` chain exists in `menu_session.rs` only for event routing, not help-context. Behavior appears not ported → gap-report. |
| `GetPalette` (method) | 484 | EQUIVALENT | OK | `MenuColors::resolve(ctx)` reading `Role::Menu*` from `Theme` | 2 | Guide: returns `CMenuView` palette (6 entries, palette indices 1–6 mapping to app palette slots 2–7). Rust: `MenuColors::resolve` reads the six `Role::Menu*` values from the draw context's theme. Known idiomatic mapping: class Palette → `tv::Theme` (D7). `MenuColors` doc explains the four pairs; the chain derivation is documented in `theme.rs` comment. Score 2: what it does; palette-chain derivation lives in theme.rs rather than at the call site. |
| `HandleEvent` (method) | 484 | EQUIVALENT | OK | `menu_view::handle_event` (passive half) + `MenuSession` (interactive half) | 3 | Guide: handles mouse, keyboard (hotkeys + Alt-shortcuts), and `cmCommandSetChanged`. Rust splits this: passive (`cmCommandSetChanged`, accelerator post, bar activation) lives in `menu_view::handle_event`; the full interactive navigation lives in `MenuSession`. Deviation D9, documented thoroughly in both module docs. Each branch is individually commented against the C++ source with line references. Score 3. |
| `HotKey` (method) | 484 | PORTED | OK | `menu_view::hot_key(menu, key) -> Option<Command>` | 3 | Guide: returns the item associated with a hot key, nil if not found or disabled. Rust: `hot_key` walks the tree recursively, skips separators, recurses into submenus, returns `None` for missing or disabled. Doc covers what (recursive walk), how (depth-first, skips disabled/separator), and heritage. Four unit tests verify the contract. Score 3. |
| `NewSubView` (method) | 485 | EQUIVALENT | OK | `MenuSession` level-push opens a `MenuBox` inline | 2 | Guide: constructs a new `TMenuBox` for a submenu. Rust: the session's level-push path creates the box geometry via `menu_box_rect` and pushes a `MenuLevel` — no separate `new_sub_view` method. Documented in `menu_session.rs` (architecture section). Score 2: explained at the architectural level; no dedicated symbol. |
| `Store` (method) | 485 | NOT-PORTED | — | — | — | TStreamable persistence. Dropped codebase-wide (deviation D12). |
| `CMenuView` palette (6 entries) | 485 | EQUIVALENT | OK | `tv::theme::Role::MenuNormal`, `MenuNormalShortcut`, `MenuSelected`, `MenuSelectedShortcut`, `MenuDisabled`, `MenuSelectedDisabled` | 2 | Guide: `CMenuView` maps indices 1–6 to app palette slots 2–7 (normal text, disabled, shortcut, normal selection, disabled selection, shortcut selection). Rust: six `Role::Menu*` variants covering the same six semantic slots; derivation chain documented in `theme.rs` comment. Known idiomatic mapping. Score 2: each role has a short "what" description; the palette-chain mapping to the 1992 guide slot numbers is not repeated in the role docs. |
| `putClickEventOnExit` (private field) | — | EQUIVALENT | OK | `MenuSession::put_click_event_on_exit: bool` | N/A | Not in guide (private field in magiblot). Session field controls exit-click re-post for bar/box vs popup. Documented in `menu_session.rs`. N/A (internal). |

## Summary

- PORTED: 4   EQUIVALENT: 7   NOT-PORTED: 2   MISSING: 1   UNSURE: 0
- SUSPECT: 0   |   doc<3 (public): 8   |   → concept: 1
- Notable finding: `GetHelpCtx` has no visible port — the guide's parent-menu chain walk is absent from `MenuSession` and no `View::get_help_ctx` override exists in either menu type. Warrants a gap-report entry. All other entries are EQUIVALENT or PORTED with documented deviations.
