# Rust build output
/target
**/*.rs.bk

# mise local state
.mise.local.toml

# Claude agent isolation worktrees (transient)
.claude/worktrees/

# stuff
*~
# mdBook build output (regenerated by `cargo xtask docs`)
docs/book/book/

# Claude scheduled-tasks lock (session-local churn)
.claude/scheduled_tasks.lock
